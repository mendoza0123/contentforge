# Verified on Python 3.14 (Windows and WSL). The original pins predate 3.14 and
# no longer build there — bump deliberately if you change them.
fastapi==0.136.1
uvicorn[standard]==0.47.0
sqlalchemy==2.0.49
pydantic==2.13.4
pydantic-settings==2.15.0
jinja2==3.1.6
python-multipart==0.0.28
openai==2.36.0
python-dotenv==1.2.2
httpx==0.28.1
psycopg[binary]==3.3.5  # Postgres (Neon) — hosts with no persistent disk need a shared DB
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==3.2.2          # passlib 1.7.4 reads bcrypt.__about__, dropped in bcrypt 4.x
pytest==8.4.2
pytest-asyncio==1.2.0
