# Normalise line endings so the same repo is clean from Windows and WSL.
* text=auto eol=lf

# Windows-only scripts want CRLF in the working tree.
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# Never touch these.
*.png binary
*.jpg binary
*.webp binary
*.db binary
